
const UserTitle = ({heading}) => {
    return (
        <div>
        <p className="text-xl font-bold text-accent mt-5">{heading}</p>
        <div className="divider"></div>
    </div>
    );
};

export default UserTitle;